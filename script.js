function firstWord(s) {
	let ans=0;
		
  // your code here
	let  arr=s.split(" ")[0];
      const wo=arr[0];
      console.log(wo);
}

// Do not change the code below

const s = prompt("Enter String:");
alert(firstWord(s));
